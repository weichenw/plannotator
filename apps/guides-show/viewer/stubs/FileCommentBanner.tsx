import React from 'react';
/** Build-time stand-in — a read-only guide carries no file comments, so this never renders. */
export const FileCommentBanner: React.FC<Record<string, unknown>> = () => null;
