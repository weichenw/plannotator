import React from 'react';
/** Build-time stand-in — a read-only guide carries no annotations, so this never renders. */
export const InlineAnnotation: React.FC<Record<string, unknown>> = () => null;
